# knn


if __name__ == '__main__':
    # run knn
    # take a list of beers a reviewer has reviewed (good and bad),
    # pick one of the beers and predict the rating the reviewer
    # gave that beer, based off of similarity to other beers that were reviewed
    # will have to fully understand scikit learn capabilities
    temp = 3
